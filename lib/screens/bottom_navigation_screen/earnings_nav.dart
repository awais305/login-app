import 'package:flutter/material.dart';

class EarningsNav extends StatelessWidget {
  const EarningsNav({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Earnings Nav'));
  }
}

import 'package:flutter/material.dart';

class JobsNav extends StatelessWidget {
  const JobsNav({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Jobs Nav'),
    );
  }
}

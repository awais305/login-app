import 'package:flutter/material.dart';

class ConsumerTab extends StatelessWidget {
  const ConsumerTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('consumer tab'));
  }
}
